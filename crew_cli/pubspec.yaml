name: crew_cli
description: CLI for Crew — publish and use experts
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  crew_core:
    path: ../crew_core
  args: ^2.5.0

dev_dependencies:
  test: ^1.25.0

executables:
  crew: crew
