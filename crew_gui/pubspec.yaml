# crew_gui/pubspec.yaml
name: crew_gui
description: Crew — assemble expert agents onto your project directories.
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.5.0
  flutter: ">=3.5.0"
dependencies:
  flutter:
    sdk: flutter
  crew_core:
    path: ../crew_core
  file_picker: ^8.0.0
  path: ^1.9.0
  path_provider: ^2.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
